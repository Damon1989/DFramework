
@{
    ViewBag.Title = "UnderScore";
}

<h2>UnderScore</h2>

@section scripts
{
    <script>
        $(function() {
            //var array=_.map([1, 2, 3],
            //    function(num) {
            //        return num * 3;
            //    });
            //console.log(array);

            //array = _.map({ one: 1, two: 2, three: 3 },
            //    function(num, key) {
            //        return num * 3;
            //    });
            //console.log(array);

            //_.each([1, 2, 3], alert);

            //alert(_.reduce([1, 2, 3], function (memo, num) { return memo + num; }, 0));

            //alert(_.every([1, 2, 3, 4, 5, 6],
            //    function(num) {
            //        return num % 2 ===0;
            //    }));

            //alert(_.some([1, 2, 3, 4, 5, 6],
            //    function (num) {
            //        return num % 2 === 0;
            //    }));

            //alert(_.size({ one: 1, two: 2, three: 3 }));

            //alert(_.filter([1, 2, 3, 4, 5, 6],
            //    function(num) {
            //        return num % 2 === 0;
            //    }));

            //alert(_.reject([1, 2, 3, 4, 5, 6],
            //    function (num) {
            //        return num % 2 === 0;
            //    }));

            //alert(_.find([1, 2, 3, 4, 5, 6],
            //    function(num) {
            //        return num % 2 === 0;
            //    }));

            //alert(_.contains([1, 2, 4], 3));

            //var info=_.countBy([1, 2, 3, 4, 5],
            //    function(num) {
            //        return num % 2 === 0 ? "even" : "odd";
            //    });
            //alert(info);
            //console.log(info);

            //console.log(_.toArray({ a: 0, b: 1, c: 2 }));

            //var stooges = [{ name: 'moe', age: 40 }, { name: "larry", age: 50 }, { name: 'curly', age: 60 }];
            //console.log(_.pluck(stooges, 'name'));

            //var o = {
            //    p: 2,
            //    m: function() {
            //        console.log(this.p);
            //    }
            //};

            //o.m();
            //console.log("-----------");
            //_.bind(o.m, { p: 1 })();


            //var add = function(n1, n2, n3) {
            //    console.log(this.sum + n1 + n2 + n3);
            //};

            //_.bind(add, { sum: 1 }, 1, 1, 1)();


            //var o = {
            //    p1: '123',
            //    p2: '456',
            //    m1: function() {
            //        console.log(this.p1);
            //    },
            //    m2: function() {
            //        console.log(this.p2);
            //    }
            //};
            //_.bindAll(o, 'm1', 'm2');


            //var add=function(a, b) {
            //    return a + b;
            //}

            //var add5 = _.partial(add, 5);
            //console.log(add5(10));

            //var hello=function(name) {
            //    return "hello:" + name;
            //}

            //hello = _.wrap(hello,
            //    function(func) {
            //        return "before," + func("moe") + ",after";
            //    });

            //hello();

            //console.log(hello());

            //var scores = [20, 81, 75, 40, 91, 59, 77, 66, 72, 88, 99];
            //var groups = _.groupBy(scores,
            //    (x) => {
            //        if (x < 60) {
            //            return "C";
            //        } else if (x < 80) {
            //            return "B";
            //        } else {
            //            return "A";
            //        }
            //    });

            //console.log(groups);

            //var names = ['Adam', 'Lisa', 'Bart'];
            //var scores = [88, 77, 55];
            //console.log(_.zip(names, scores));

            //console.log(_.object(names, scores));

            //var s = " Hello ";
            //console.log(s);
            //console.log(s.trim());

            //_.delay(alert, 2000);

            var log = _.bind(console.log, console);
            _.delay(log, 2000, "hello", "world", 11);

            function Student(name, age) {
                this.name = name;
                this.age = age;
            }

            var xiaoming = new Student("小明", 20);
            console.log(_.keys(xiaoming));
            console.log(_.values(xiaoming));
        })
    </script>
}
